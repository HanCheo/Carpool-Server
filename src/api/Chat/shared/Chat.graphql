type Chat {
    id: Int!
    messages: [Message]
    passenger: User!
    driver: User!
    passengerId: Int!
    driverId: Int!
    rideId: Int!
    ride:Ride!
    createAt: String!
    updateAt: String
}